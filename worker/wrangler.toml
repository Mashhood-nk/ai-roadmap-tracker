name = "ai-roadmap-worker"
main = "src/index.js"
compatibility_date = "2024-01-01"

# D1 Database binding
[[d1_databases]]
binding = "DB"
database_name = "ai-roadmap-db"
database_id = "REPLACE_WITH_YOUR_D1_DATABASE_ID"

# APP_PIN is set as a secret — never put it here
# Run: wrangler secret put APP_PIN
